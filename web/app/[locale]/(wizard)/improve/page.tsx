import { PlaceholderStep } from "@/components/wizard/placeholder-step";

/** Step 4 — `/[locale]/improve`. Enterable once a simulation succeeded. */
export default function ImprovePage() {
  return <PlaceholderStep slug="improve" />;
}
