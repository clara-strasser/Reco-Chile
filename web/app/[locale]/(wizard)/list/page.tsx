import { ListStep } from "@/components/wizard/list-step";

/** Step 2 — `/[locale]/list`. Enterable once the RUN/IPE pre-check passes. */
export default function ListPage() {
  return <ListStep />;
}
