import { PlaceholderStep } from "@/components/wizard/placeholder-step";

/** Step 3 — `/[locale]/result`. Enterable once the list holds a valid wish. */
export default function ResultPage() {
  return <PlaceholderStep slug="result" />;
}
